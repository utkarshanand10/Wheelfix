/**
 * createAdmin.js
 * DEPRECATED: This script is kept for historical reasons but has been
 * consolidated into `scripts/seedAdmin.js` which supports environment
 * variables for admin credentials and creates sample data.
 *
 * Use `node scripts/seedAdmin.js` or `node createAdmin.js` (deprecated).
 */

console.log("createAdmin.js is deprecated.");
console.log(
  "Please use `node scripts/seedAdmin.js` or set ADMIN_EMAIL/ADMIN_PASSWORD in .env and run `node createAdmin.js` if you need a single admin."
);

process.exit(0);
